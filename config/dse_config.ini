[General]
; Number of years of history to fetch on first symbol load
HistoryYears=3

; Polling interval in milliseconds during market hours
PollIntervalMs=5000

; DSE Market hours (Bangladesh Standard Time, UTC+6)
MarketOpenHour=10
MarketOpenMinute=0
MarketCloseHour=14
MarketCloseMinute=30

; Maximum reconnection attempts before giving up
MaxReconnectAttempts=10

; HTTP request timeout in seconds
HttpTimeoutSec=30

; User agent string for HTTP requests
UserAgent=Mozilla/5.0 (Windows NT 10.0; Win64; x64) AppleWebKit/537.36

; 1 = Web data overwrites local CSV seed | 0 = local CSV takes priority
PreferWebData=1

[Endpoints]
; DSE latest share price page (HTML table)
LatestPrice=https://www.dsebd.org/latest_share_price_scroll_l.php

; DSE day-end archive for historical OHLCV data
DayEndArchive=https://www.dsebd.org/day_end_archive.php

; Alternative endpoint (reserved)
AltLatestPrice=https://www.dsebd.org/latest_share_price_all_,ajax.php

[DataSource]
; Folder containing per-symbol CSV seed files (e.g. GP.csv, BRAC.csv)
; These are used as the primary historical data source before web fetch.
; Leave empty to use web-only mode.
CsvSeedPath=d:\software\dse_2000-2025\separated_data

[Export]
; Folder to export cached data as CSV files (leave empty to disable)
ExportPath=

; Auto-export interval in seconds (0 = manual only via "Save CSVs Now" button)
ExportIntervalSec=0

[Debug]
; Enable debug logging to file (0=off, 1=on)
EnableLogging=0
LogFilePath=dse_plugin.log
